import{_ as t,o,c as i,ag as s}from"./chunks/framework.DQQHyi2x.js";const h=JSON.parse('{"title":"Config","description":"","frontmatter":{},"headers":[],"relativePath":"features/config.md","filePath":"features/config.md"}'),a={name:"features/config.md"};function d(r,e,n,l,c,u){return o(),i("div",null,[...e[0]||(e[0]=[s('<h1 id="config" tabindex="-1">Config <a class="header-anchor" href="#config" aria-label="Permalink to &quot;Config&quot;">​</a></h1><p>The Config page lets you view, edit, and understand provider configuration. Claude Code uses JSON settings across multiple scopes; Codex CLI uses TOML files under <code>$CODEX_HOME</code>.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Claude Code stores settings in multiple files at different scopes. The Claude Code config page provides three views:</p><ul><li><strong>Settings Editor</strong> — form-based editor for common settings</li><li><strong>Scope Resolver</strong> — shows how settings merge from different scopes</li><li><strong>Raw Viewer</strong> — displays actual configuration files</li></ul><h2 id="how-to-use" tabindex="-1">How to Use <a class="header-anchor" href="#how-to-use" aria-label="Permalink to &quot;How to Use&quot;">​</a></h2><h3 id="settings-editor" tabindex="-1">Settings Editor <a class="header-anchor" href="#settings-editor" aria-label="Permalink to &quot;Settings Editor&quot;">​</a></h3><p>The Settings Editor tab provides form controls for common Claude Code settings:</p><table tabindex="0"><thead><tr><th>Setting</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td>Model</td><td>Dropdown</td><td>Claude model selection (Opus 4.6, Sonnet 4.6, Haiku 4.5, etc.)</td></tr><tr><td>Permission Mode</td><td>Dropdown</td><td>default, acceptEdits, dontAsk, plan, bypassPermissions, delegate</td></tr><tr><td>Update Channel</td><td>Dropdown</td><td>stable or latest</td></tr><tr><td>Teammate Mode</td><td>Dropdown</td><td>auto, in-process, or tmux</td></tr><tr><td>Read-only Files</td><td>Toggle</td><td>Always allow reading files without permission prompts</td></tr><tr><td>Timeouts &amp; Limits</td><td>Number inputs</td><td>Various timeout and limit settings</td></tr></tbody></table><p>The editor also includes a <strong>Permission Rules</strong> section where you can add allow/ask/deny patterns using glob syntax.</p><h3 id="scope-resolver" tabindex="-1">Scope Resolver <a class="header-anchor" href="#scope-resolver" aria-label="Permalink to &quot;Scope Resolver&quot;">​</a></h3><p>The Scope Resolver tab shows how Claude Code merges settings from four sources (highest to lowest priority):</p><ol><li><strong>Local</strong> — <code>~/.claude/settings.local.json</code> (not committed)</li><li><strong>Project</strong> — <code>.claude/settings.json</code> (project-specific)</li><li><strong>User</strong> — <code>~/.claude/settings.json</code> (user-wide)</li><li><strong>Managed</strong> — Enterprise-enforced settings (read-only)</li></ol><p>For each setting, you can see which scope provides the active value and use the &quot;Override in local&quot; button to copy a value to your local scope.</p><h3 id="raw-viewer" tabindex="-1">Raw Viewer <a class="header-anchor" href="#raw-viewer" aria-label="Permalink to &quot;Raw Viewer&quot;">​</a></h3><p>The Raw Viewer tab shows a file list on the left and file content on the right:</p><ul><li><code>~/.claude.json</code> — OAuth tokens, caches, MCP servers</li><li><code>~/.claude/settings.json</code> — User settings and permissions</li><li><code>.claude/settings.json</code> — Project settings</li><li><code>.mcp.json</code> — Project MCP servers</li><li><code>CLAUDE.md</code> — Project instructions</li><li>Command files from <code>~/.claude/commands/</code> and <code>.claude/commands/</code></li></ul><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Sensitive values (tokens, secrets, passwords, API keys) are automatically masked in the merged settings view.</p></div><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>The Config page reads and writes to the standard Claude Code configuration files. No additional Claude Deck configuration is needed.</p><h2 id="codex-cli" tabindex="-1">Codex CLI <a class="header-anchor" href="#codex-cli" aria-label="Permalink to &quot;Codex CLI&quot;">​</a></h2><p>When the selected provider is Codex CLI, the Config page switches to Codex-specific cards:</p><ul><li>General settings such as model, reasoning effort, and profile</li><li>Runtime settings such as sandbox, approval policy, search, strict config, and alternate screen behavior</li><li>Feature flags reported by <code>codex features list</code></li><li>Project trust entries</li><li>Profile v2 files</li><li>Rules files</li><li>MCP and plugin inventory</li><li>Diagnostics from <code>codex doctor</code></li></ul><p>Codex config writes are intentionally narrower than Claude Code settings. Claude Deck only updates whitelisted TOML keys, creates a backup before saving, and refuses unsafe paths. Auth, history, model cache, and log files are not shown in the raw viewer.</p><h3 id="codex-settings-editor" tabindex="-1">Codex Settings Editor <a class="header-anchor" href="#codex-settings-editor" aria-label="Permalink to &quot;Codex Settings Editor&quot;">​</a></h3><p>The Codex editor keeps open-ended fields editable and uses dropdowns where Codex has known values:</p><table tabindex="0"><thead><tr><th>Setting</th><th>Control</th><th>Notes</th></tr></thead><tbody><tr><td>Model</td><td>Text input</td><td>Shows model ids already found in the loaded config/profile data, but allows any Codex-supported model id.</td></tr><tr><td>Reasoning Effort</td><td>Dropdown</td><td>Default, low, medium, high, or extra high. Existing custom values remain selectable.</td></tr><tr><td>Profile</td><td>Text input</td><td>Shows profile names found in config/profile diagnostics, but allows any Codex profile name.</td></tr><tr><td>Sandbox Mode</td><td>Dropdown</td><td>Default, read-only, workspace-write, or danger-full-access.</td></tr><tr><td>Approval Policy</td><td>Dropdown</td><td>Default, untrusted, on-request, never, or deprecated on-failure.</td></tr><tr><td>Search</td><td>Toggle</td><td>Enables live web search for Codex.</td></tr><tr><td>Strict Config</td><td>Toggle</td><td>Makes Codex reject unrecognized config fields.</td></tr><tr><td>No Alt Screen</td><td>Toggle</td><td>Runs the Codex TUI inline so terminal scrollback is preserved.</td></tr></tbody></table><p>Settings and feature labels include help icons. When an official description is known, the tooltip explains the setting. When a Codex feature flag has no public description, Claude Deck says so and still shows the stage and current effective state reported by the installed CLI.</p><h3 id="codex-feature-flags" tabindex="-1">Codex Feature Flags <a class="header-anchor" href="#codex-feature-flags" aria-label="Permalink to &quot;Codex Feature Flags&quot;">​</a></h3><p>The Features card lists active, non-deprecated flags from <code>codex features list</code>. The value shown is the effective value from Codex unless you explicitly override it in <code>config.toml</code>.</p><p>Common documented flags include:</p><ul><li><code>goals</code></li><li><code>memories</code></li><li><code>hooks</code></li><li><code>multi_agent</code></li><li><code>network_proxy</code></li><li><code>shell_tool</code></li><li><code>shell_snapshot</code></li><li><code>unified_exec</code></li><li><code>prevent_idle_sleep</code></li></ul><p>Use the reset button next to a configured flag to remove the explicit override and return to the Codex default.</p><p>Profile diagnostics resolve default and active profiles, profile files, inherited and overridden settings, and missing or malformed profile files. Summaries redact secret-like values and avoid exposing auth, history, or cache data.</p><p>History and model-cache diagnostics are privacy-bounded. They may report file existence, size, parse state, root keys, row counts, and metric-like key names, but they do not return prompt text, session ids, raw history rows, model ids, raw model cache payloads, or SQLite contents.</p><p>Usage and context parity are not supported for Codex. Claude Code usage and context pages remain Claude-specific until Codex exposes a stable metric source that can be read without sensitive prompt or cache data.</p><p>Codex backups are export-only in this version. They include redacted config, profile files, rules, and provider inventory metadata, but automatic restore is disabled.</p><h2 id="tips" tabindex="-1">Tips <a class="header-anchor" href="#tips" aria-label="Permalink to &quot;Tips&quot;">​</a></h2><ul><li>Use the <strong>Scope Resolver</strong> to understand why a setting has a particular value — it shows the source scope.</li><li><strong>Local overrides</strong> (<code>settings.local.json</code>) take highest priority and are not committed to version control.</li><li>Permission patterns support glob syntax. The editor validates patterns and suggests fixes for deprecated syntax.</li></ul>',39)])])}const g=t(a,[["render",d]]);export{h as __pageData,g as default};
