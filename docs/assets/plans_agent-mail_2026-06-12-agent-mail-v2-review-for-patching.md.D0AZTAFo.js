import{_ as s,o as i,c as a,ag as t}from"./chunks/framework.DQQHyi2x.js";const k=JSON.parse('{"title":"Agent Mail v2 Review For Plan Patching","description":"","frontmatter":{},"headers":[],"relativePath":"plans/agent-mail/2026-06-12-agent-mail-v2-review-for-patching.md","filePath":"plans/agent-mail/2026-06-12-agent-mail-v2-review-for-patching.md"}'),n={name:"plans/agent-mail/2026-06-12-agent-mail-v2-review-for-patching.md"};function o(l,e,d,p,r,h){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="agent-mail-v2-review-for-plan-patching" tabindex="-1">Agent Mail v2 Review For Plan Patching <a class="header-anchor" href="#agent-mail-v2-review-for-plan-patching" aria-label="Permalink to &quot;Agent Mail v2 Review For Plan Patching&quot;">​</a></h1><p><strong>Reviewed plan:</strong> <code>docs/plans/agent-mail/2026-06-12-agent-mail-v2-implementation-plan.md</code><strong>Purpose:</strong> Actionable review notes for another agent to patch the v2 implementation plan before coding starts. <strong>Review verdict:</strong> Close to implementation-ready, but not suitable to execute literally until the blocking issues below are patched.</p><hr><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>The v2 plan is a strong improvement over the original. It has a clearer product shape, durable per-repo team members, structured context requests and handoffs, state-based delivery, and a practical non-chat UI.</p><p>The implementation sequence is also mostly usable: it is test-first, references current repo conventions, and decomposes backend, MCP, hooks, install, frontend, and docs in a reasonable order.</p><p>However, several details would either fail during implementation or ship behavior that does not match the stated product goals.</p><p>Patch the plan before assigning implementation.</p><hr><h2 id="blocking-issues" tabindex="-1">Blocking Issues <a class="header-anchor" href="#blocking-issues" aria-label="Permalink to &quot;Blocking Issues&quot;">​</a></h2><h3 id="_1-hook-install-code-omits-required-scope" tabindex="-1">1. Hook install code omits required <code>scope</code> <a class="header-anchor" href="#_1-hook-install-code-omits-required-scope" aria-label="Permalink to &quot;1. Hook install code omits required \`scope\`&quot;">​</a></h3><p><strong>Severity:</strong> Blocking implementation defect</p><p>The plan creates <code>HookCreate(...)</code> in Task 8 without <code>scope</code>. In the current repo, <code>HookCreate.scope</code> is required.</p><p>Relevant v2 plan area:</p><ul><li>Task 8 install service</li><li><code>apply_claude_code_install</code></li><li><code>HookCreate(...)</code> block around the hook install loop</li></ul><p>Current repo facts:</p><ul><li><code>backend/app/models/schemas.py</code> defines <code>HookCreate.scope: str</code></li><li><code>backend/app/services/hook_service.py</code> branches on <code>hook.scope</code></li></ul><p>Required patch:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">HookCreate(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    event</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">event,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    matcher</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">POST_TOOL_USE_MATCHER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;PostToolUse&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> else</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> None</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    command</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">hook_command(slug),</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    scope</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Also patch <code>test_apply_adds_missing_hooks_and_mcp</code> to assert every added hook has <code>scope == &quot;user&quot;</code>.</p><p>Why this matters:</p><p>Without this, Task 8 fails at runtime when constructing <code>HookCreate</code>.</p><hr><h3 id="_2-handoffs-never-close-when-acked" tabindex="-1">2. Handoffs never close when acked <a class="header-anchor" href="#_2-handoffs-never-close-when-acked" aria-label="Permalink to &quot;2. Handoffs never close when acked&quot;">​</a></h3><p><strong>Severity:</strong> Blocking behavior defect</p><p>The plan models <code>handoff</code> as a request kind with status <code>pending</code>, but <code>ack_message</code> only moves a root request to <code>acknowledged</code> when the acked message is an <code>answer</code> and the root sender is acknowledging that answer.</p><p>That means a recipient can ack a handoff, but the handoff root remains <code>pending</code> and keeps contributing to pending counts.</p><p>Relevant v2 plan areas:</p><ul><li>Task 4 messaging service</li><li><code>MAIL_REQUEST_KINDS = [&quot;context_request&quot;, &quot;handoff&quot;]</code></li><li><code>counts_for_member</code></li><li><code>ack_message</code></li><li>MCP <code>deck_create_handoff</code></li><li>MCP <code>deck_ack_message</code></li></ul><p>Required patch:</p><p>Add behavior to <code>ack_message</code>:</p><ul><li>If the acked message is a root <code>handoff</code></li><li>And <code>message.recipient_member_id == member_id</code></li><li>And <code>message.request_status == &quot;pending&quot;</code></li><li>Then set <code>message.request_status = &quot;acknowledged&quot;</code></li></ul><p>Suggested service logic:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    message </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">is</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> not</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> None</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    and</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message.kind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;handoff&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    and</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message.thread_root_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">is</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> None</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    and</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message.recipient_member_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> member_id</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    and</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message.request_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;pending&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    message.request_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;acknowledged&quot;</span></span></code></pre></div><p>Required tests:</p><ol><li><code>test_handoff_ack_closes_request</code></li><li>Confirm <code>counts_for_member</code> pending count drops after ack.</li><li>Confirm unrelated members cannot close the handoff by acking.</li></ol><p>Why this matters:</p><p>The plan presents handoffs as structured requests. If acking a handoff does not close it, the core request lifecycle is inconsistent.</p><hr><h3 id="_3-codex-one-click-install-is-deferred-despite-existing-codex-mcp-mutation-support" tabindex="-1">3. Codex one-click install is deferred despite existing Codex MCP mutation support <a class="header-anchor" href="#_3-codex-one-click-install-is-deferred-despite-existing-codex-mcp-mutation-support" aria-label="Permalink to &quot;3. Codex one-click install is deferred despite existing Codex MCP mutation support&quot;">​</a></h3><p><strong>Severity:</strong> Product-goal mismatch</p><p>The v2 goal says local Claude Code and Codex CLI sessions should coordinate with one-click install. But Task 8 says Codex only gets copy-paste <code>config.toml</code> and <code>AGENTS.md</code> snippets because <code>codex_config_service</code> has no MCP mutation seam.</p><p>The repo already exposes Codex MCP mutation through provider endpoints:</p><ul><li><code>POST /api/v1/providers/{provider_id}/mcp</code></li><li>Backed by Codex CLI <code>codex mcp add</code></li><li>Provider capabilities classify Codex MCP as write-capable.</li></ul><p>Required decision:</p><p>Choose one and update the plan clearly:</p><ol><li><strong>Preferred:</strong> Include Codex one-click MCP install in MVP.</li><li><strong>Acceptable but weaker:</strong> Keep Codex manual snippets, but revise the top-level goal and acceptance criteria so &quot;one-click install&quot; explicitly means Claude Code only in MVP.</li></ol><p>Recommended patch if choosing one-click Codex MCP:</p><ul><li>Add Codex MCP install status to <code>AgentMailInstallStatus</code>.</li><li>Add install endpoint:</li></ul><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>POST /api/v1/agent-mail/install/codex/apply</span></span></code></pre></div><ul><li>Have install service call the existing Codex provider MCP mutation path or extract shared helper logic.</li><li>Keep <code>AGENTS.md</code> as a snippet unless there is a safe existing writer.</li></ul><p>Required tests:</p><ul><li>Codex MCP install builds the same command/sys.executable and shim path.</li><li>Codex MCP install uses <code>CLAUDE_DECK_PROVIDER=codex-cli</code>.</li><li>Codex hook status remains unsupported/not installed.</li></ul><p>Why this matters:</p><p>The feature promise is cross-provider agent coordination. Manual Codex setup weakens that promise and conflicts with the earlier product goal.</p><hr><h3 id="_4-install-has-no-preview-confirmation-or-backup-path" tabindex="-1">4. Install has no preview, confirmation, or backup path <a class="header-anchor" href="#_4-install-has-no-preview-confirmation-or-backup-path" aria-label="Permalink to &quot;4. Install has no preview, confirmation, or backup path&quot;">​</a></h3><p><strong>Severity:</strong> Safety/product risk</p><p>Task 8 directly mutates real user config through <code>HookService.add_hook</code> and <code>MCPService.add_server</code>. That matches current service mechanics, but it is risky for an install button that edits <code>~/.claude/settings.json</code> and <code>~/.claude.json</code>.</p><p>Relevant v2 plan areas:</p><ul><li>Task 8 install service</li><li>Install tab &quot;Install&quot; button</li><li>Docs security section</li></ul><p>Required decision:</p><p>Either:</p><ol><li>Add preview/confirmation/backup to MVP, or</li><li>Explicitly accept current app semantics and document that Agent Mail install follows existing Hooks/MCP writer behavior without backup.</li></ol><p>Recommended patch:</p><ul><li>Add <code>GET /agent-mail/install/status</code></li><li>Add <code>POST /agent-mail/install/preview</code></li><li>Add <code>POST /agent-mail/install/claude-code/apply</code> with <code>{ confirmed: true }</code></li><li>Return planned files and operations:</li></ul><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;changes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;file&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/.claude/settings.json&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;operation&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;add_hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;file&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/.claude.json&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;operation&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;add_mcp_server&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;warnings&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>If backup helpers exist, use them. If not, at least write a timestamped <code>.bak</code> before mutation or document why not.</p><p>Required tests:</p><ul><li>Preview does not mutate files/services.</li><li>Apply requires confirmation.</li><li>Apply only adds missing Agent Mail hooks/server.</li><li>Uninstall only removes hooks whose command contains <code>/api/v1/agent-mail/hooks/</code>.</li></ul><p>Why this matters:</p><p>Claude Deck edits real local agent configuration. Install UX should be trustworthy.</p><hr><h3 id="_5-hook-output-delivery-assumption-is-verified-too-late" tabindex="-1">5. Hook-output delivery assumption is verified too late <a class="header-anchor" href="#_5-hook-output-delivery-assumption-is-verified-too-late" aria-label="Permalink to &quot;5. Hook-output delivery assumption is verified too late&quot;">​</a></h3><p><strong>Severity:</strong> Architectural risk</p><p>The plan depends on <code>curl</code> command hook stdout being parsed by Claude Code as JSON containing <code>hookSpecificOutput.additionalContext</code>.</p><p>The plan lists this as a known risk at the end, but by then most of the feature has already been built.</p><p>Required patch:</p><p>Move this to an early spike before full backend/UI implementation.</p><p>Add a new early task after Task 1 or before Task 7:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Task X: Verify curl command-hook additionalContext delivery</span></span></code></pre></div><p>Minimum acceptance:</p><ol><li>Install a temporary command hook manually or through a small test snippet.</li><li>The command emits JSON on stdout:</li></ol><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hookSpecificOutput&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;hookEventName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SessionStart&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;additionalContext&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Agent Mail smoke test context&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ol start="3"><li>Start Claude Code and confirm the context is visible to the model.</li><li>If it fails, switch the plan to a tiny wrapper script before continuing.</li></ol><p>Why this matters:</p><p>This is the core delivery mechanism. If it is wrong, the rest of the plan still builds but the most important behavior does not work.</p><hr><h2 id="non-blocking-but-important-issues" tabindex="-1">Non-Blocking But Important Issues <a class="header-anchor" href="#non-blocking-but-important-issues" aria-label="Permalink to &quot;Non-Blocking But Important Issues&quot;">​</a></h2><h3 id="_6-global-visibility-may-become-noisy" tabindex="-1">6. Global visibility may become noisy <a class="header-anchor" href="#_6-global-visibility-may-become-noisy" aria-label="Permalink to &quot;6. Global visibility may become noisy&quot;">​</a></h3><p>The plan explicitly removes scopes/workspaces in MVP and makes all members mutually visible. This is acceptable for a first release if documented, but the UI should include filters from the start.</p><p>Recommended patch:</p><ul><li>Team tab should support filters: all, connected, observed, offline, repo name search.</li><li>Requests tab should support filters: pending, answered, acknowledged, stale.</li><li>Docs should say machine-global visibility is the MVP behavior.</li></ul><h3 id="_7-one-member-per-repo-is-clear-but-limiting" tabindex="-1">7. One member per repo is clear but limiting <a class="header-anchor" href="#_7-one-member-per-repo-is-clear-but-limiting" aria-label="Permalink to &quot;7. One member per repo is clear but limiting&quot;">​</a></h3><p>The plan keys durable members by <code>repo_id</code>, which means two agents in the same repo share a single member identity. The plan calls this out as MVP behavior.</p><p>Recommended patch:</p><ul><li>Make this limitation visible in docs and UI copy.</li><li>In smoke tests, include two sessions in the same repo and verify they attach to one member.</li></ul><h3 id="_8-mcp-and-hook-sessions-may-duplicate-the-same-real-agent-in-the-ui" tabindex="-1">8. MCP and hook sessions may duplicate the same real agent in the UI <a class="header-anchor" href="#_8-mcp-and-hook-sessions-may-duplicate-the-same-real-agent-in-the-ui" aria-label="Permalink to &quot;8. MCP and hook sessions may duplicate the same real agent in the UI&quot;">​</a></h3><p>The plan already calls this cosmetic. That is acceptable, but the Team tab should render sessions compactly under one member and avoid making duplicates look like separate teammates.</p><p>Recommended patch:</p><ul><li>Session list shows <code>source</code> badges: hook, mcp, observed.</li><li>Member status derives from best session status.</li></ul><h3 id="_9-tests-use-raw-len-result-scalars-all" tabindex="-1">9. Tests use raw <code>len(result.scalars().all())</code> <a class="header-anchor" href="#_9-tests-use-raw-len-result-scalars-all" aria-label="Permalink to &quot;9. Tests use raw \`len(result.scalars().all())\`&quot;">​</a></h3><p>This is fine for MVP scale, but counting receipts/messages in service code can be more efficient with SQL count queries.</p><p>Recommended patch:</p><ul><li>Optional. Do not block implementation.</li></ul><h3 id="_10-dependency-addition-should-update-both-dependency-manifests-if-needed" tabindex="-1">10. Dependency addition should update both dependency manifests if needed <a class="header-anchor" href="#_10-dependency-addition-should-update-both-dependency-manifests-if-needed" aria-label="Permalink to &quot;10. Dependency addition should update both dependency manifests if needed&quot;">​</a></h3><p>The plan adds <code>mcp&gt;=1.2.0</code> to <code>backend/requirements.txt</code>. The repo also has <code>backend/pyproject.toml</code> and <code>backend/uv.lock</code>.</p><p>Recommended patch:</p><ul><li>Add <code>mcp</code> to <code>backend/pyproject.toml</code> too, or explicitly state that <code>requirements.txt</code> is the only install source for this project.</li><li>If the project expects lockfile maintenance, run the relevant lock command.</li></ul><hr><h2 id="suggested-patch-checklist" tabindex="-1">Suggested Patch Checklist <a class="header-anchor" href="#suggested-patch-checklist" aria-label="Permalink to &quot;Suggested Patch Checklist&quot;">​</a></h2><p>Ask the patching agent to update the v2 plan as follows:</p><ol><li>Add <code>scope=&quot;user&quot;</code> to every <code>HookCreate</code> in Task 8.</li><li>Add tests asserting hook scope.</li><li>Add handoff ack lifecycle logic and tests.</li><li>Decide Codex one-click MCP install vs explicit deferral.</li><li>If including Codex one-click, add install service/API/UI/tests for it.</li><li>Add install preview/confirmation and backup or explicitly document why MVP follows existing direct-write behavior.</li><li>Move curl hook-output verification into an early spike task.</li><li>Add UI filters for team/request noise control.</li><li>Document one-member-per-repo and machine-global visibility as MVP limitations.</li><li>Clarify dependency update procedure for <code>mcp</code>.</li></ol><hr><h2 id="implementation-readiness-after-patching" tabindex="-1">Implementation Readiness After Patching <a class="header-anchor" href="#implementation-readiness-after-patching" aria-label="Permalink to &quot;Implementation Readiness After Patching&quot;">​</a></h2><p>After the blocking issues are patched, the plan should be suitable for implementation.</p><p>The strongest parts to preserve:</p><ul><li>Durable member vs ephemeral session split.</li><li>State-based delivery instead of event delivery.</li><li>Context request and handoff as first-class message kinds.</li><li>MCP tool responses piggybacking unread/pending counts.</li><li>Non-chat UI centered on Team, Requests, and Install.</li></ul><p>Do not revert to the original Agent Mail v1 architecture unless the hook delivery spike fails and the project chooses to simplify delivery.</p>`,119)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
